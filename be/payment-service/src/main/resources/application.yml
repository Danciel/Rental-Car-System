server:
  port: 8084

service:
  user:
    url: http://localhost:8081
  car:
    url: http://localhost:8082

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:postgresql://localhost:5433/payment_db
    #   TODO: use env vars for username and password
    username: p2p_admin
    password: secretpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create # Test environment, use 'update' or 'validate' for production
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false

jwt:
  # ${T�N_BI?N_ENV:Gi�_tr?_m?c_??nh_n?u_kh�ng_t�m_th?y}
  secret: ${JWT_SECRET:Y2h1b2lfYmlfbWF0X3NpZXVfY2FwX2N1YV9oZV90aG9uZ19taWNyb3NlcnZpY2VzX3AycA}